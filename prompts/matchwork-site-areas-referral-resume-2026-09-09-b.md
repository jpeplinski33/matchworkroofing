DONE 2026-09-10 — superseded by prompts/matchwork-gbp-plan-resume-2026-09-10-a.md. The site work in this brief SHIPPED as commit bf614bc. Do not launch from this file.

You are Claude Fable 5.1, QB on the MATCHWORK ORIGINAL SITE: SERVICE AREAS + REFERRAL PROGRAM for Jordan. Resuming from session 291fd600-9fd2-42c1-aa69-fa6501c6776b (2026-09-10 11:25 EDT, context rollover at +101K). Effort HIGH. Other seats OFF; session 3ae2f731 is finished — if a NEWER interactive session.lock.json appears for this cwd, STOP. MODE: this is a headless `claude -p` leg started by launch-next-agent.sh — Jordan cannot reply here; output goes to <cwd>/logs/<stamp>-Claude.log; speak milestones with direct `say -r 190` and never wait for an answer. Print the origin banner first (previous session 291fd600…, ended 2026-09-10 11:25 EDT).

STEP 0: arm the watcher, then report the counter. SID=$(sed -n 's/^session_id=//p' "$PWD/.ai-session-notes"); python3 ~/ai-brain/config/scripts/context-watch.py --session "$SID" --cwd "$PWD" --dry-run; nohup python3 ~/ai-brain/config/scripts/context-watch.py --session "$SID" --cwd "$PWD" >/dev/null 2>&1 & — then in every substantive reply report exactly: Context: <abs>K abs · +<work>K above <baseline>K baseline (handoff +60K · rotate +80K · hard stop +100K), from the dry-run or ~/.ai-session-doctrine/claude/<sid>/context.log; never estimate. FAN-OUT RULE (HARD): partition delegable work and launch all partitions at once; thin QB, subagents write to disk, returns ≤300 words.

READ FIRST: /Users/jpmackbookpro/Projects/matchworkroofing/HANDOFF.md (rulings, verified state, the exact encrypt procedure, Newman's lists), then docs/README-GATE.md, then site-src/docs/index.html (the ORIGINAL site is plain Tailwind-CDN HTML with no build step; site-src/docs is the source). Verify: `git log --oneline -2` shows 8b43913 on main; tag approved-2026-09-09 = 251852f.

RULINGS (Jordan, verbatim intent): The original site is the base and the branding direction — "this look feel and vibe I like"; do not change look, feel, typography or voice. The four-trade mock-v2 build is rejected; never redeploy it. Contact email is info@matchworkroofing.com, never his name (done). He wants MORE SERVICE AREAS listed, modeled on newmanroofing.com/service-area/additional-service-areas/, because the goal is the Google Maps 3-pack. He wants a REFERRAL PROGRAM like newmanroofing.com/referral/. Gate stays ON; passphrase only ever in the STATICRYPT_PASSWORD env var, never in a file, commit or chat.

TASK: (1) Service areas: add a "Service Areas" section to index.html and a new /service-areas.html page in the site's existing Tailwind style: primary areas linked (Columbus + the 5 existing location pages), then an additional-areas list of Central Ohio communities (Newman's 25 as the reference; use the HANDOFF list; skip the far-out Knox/Morrow/Fairfield towns unless the radius is clearly Matchwork's), then "Don't see your area? Call (614) 741-1393." Add the page to sitemap.xml and nav/footer. (2) Referral: build /referral.html modeled on Newman ("Refer a Friend" shape, form fields per HANDOFF); wire the form exactly the way the site's existing forms submit (inspect first — no fake endpoint); reward line = Newman's default ($100 gift card when the appointment is scheduled) but FLAG it to Jordan as his money decision; no invented fine print; §0 bans still apply (no fake reviews, no 555 numbers, no personal name, no certif*, no Owens Corning, no financing copy). (3) Encrypt per HANDOFF (output lands in <scratch>/docs/), assert 0 plaintext html in docs/, commit with a clear message, push origin main, poll `gh api repos/{owner}/{repo}/pages/builds/latest` until built, verify via a DoH-resolved --resolve fetch (local DNS is sinkholed — non-evidence). (4) Tell Jordan honestly, in the log AND by `say`: the list is live, and a website list alone does not move the Maps 3-pack — GBP service areas, reviews and proximity do; the same list goes into the GBP plan. Show every new/changed copy line before/after in the log. (5) Update HANDOFF.md, OS-DECISIONS (~/Projects/matchwork-website-revamp-2026-09-06/deliverables/OS-DECISIONS-2026-09-09.md), the memory file ~/ai-brain/memory/matchwork-site-gated-trades-deploy-2026-09-09.md with the new SHA, and the scorecard at ~/.ai-session-doctrine/claude/<sid>/scorecard.json.

GUARDRAILS: never delete or move originals (dated backup of docs/ under logs/ before rsync); docs/ never hand-edited; one owner per file; no DNS/Cloudflare/CNAME/_headers/gate-template changes; no spend; no GBP creation; no force-push or history rewrite; no mock-v2 deploy. Pushes to main are authorized while the gate is ON (OS §8.10 amendment). Human-only gates: gate removal, DNS, spend, reward amount final, password resets. CONTEXT ROLLOVER (HARD): handoff at +60K work above baseline, rotate by +80K, never past +100K — write HANDOFF.md + prompts/<job>-resume-<YYYY-MM-DD>-<letter>.md (the filename MUST contain "resume") from ~/ai-brain/skills/_shared/rollover-handoff-template.md, paste the resume prompt in ONE code block (backup record only), append ~/ai-brain/rollovers.log. AUTO-LAUNCH (HARD, Jordan 2026-09-03 00:08 EDT): then LAUNCH THE SUCCESSOR YOURSELF — first `claude auth status 2>/dev/null | python3 -c 'import json,sys;print(json.load(sys.stdin).get("loggedIn"))'` (must print True), then `printf 'by-agent %s\n' "$(date -u +%FT%TZ)" > ~/.ai-session-doctrine/claude/<sid>/ROLLOVER-LAUNCHED`, then `nohup /bin/bash ~/ai-brain/config/scripts/launch-next-agent.sh --dir <cwd> --agent Claude --prompt-file <abs resume prompt> >/dev/null 2>&1 &`. The marker goes first and the nohup/& form is required. Never hand Jordan a prompt to paste, never wait for him. If loggedIn is not True: `say "Claude CLI is logged out, run claude login"`, record it in HANDOFF, and run only the next atomic step as a fresh-context fable-high background subagent with this whole prompt as its brief, re-probing auth every turn. Then END THE TURN.
