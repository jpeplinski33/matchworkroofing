# Matchwork website — public as authorized September 11, 2026

Jordan authorized removing the password so prospects can use the cleaned site immediately.
`site-src/docs/` is the editable source; `docs/` is the published copy, now plaintext HTML.
Run `python3 tools/verify-site.py` before publication and require docs files to match source bytes.
Read root FACTS.md: no unsupported warranty, manufacturer certification, financing, licensing, bonding or insured-company claims. CertainTeed product names are permitted.
The old gate configuration/template is retained in Git for rollback; it does not protect the now-public website.
Last gated candidate: 177899b. Historical source and rejected drafts must not overwrite the current site.
Do not invoke the old gated build helper after launch. Keep assets/fonts local and preserve their OFL license notices.
The retired portfolio URL redirects home; the map and invented jobs remain removed. Contact uses working phone/email links, not an unconnected form.
